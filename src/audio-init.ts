import button from'./assets/audio/button.ogg';import correct from'./assets/audio/correct.ogg';import wrong from'./assets/audio/wrong.ogg';import complete from'./assets/audio/complete.ogg';import mission from'./assets/audio/mission.ogg';import badge from'./assets/audio/badge.ogg';
const sounds={button,correct,wrong,complete,mission,badge};function play(src:string){if(localStorage.getItem('sound')==='false')return;const a=new Audio(src);a.volume=.32;void a.play().catch(()=>undefined)}document.addEventListener('click',e=>{const el=(e.target as HTMLElement).closest('button,a');if(!el)return;const t=el.textContent||'';if(/破案|放回/.test(t))play(sounds.complete);else if(/進入關卡|開始探險/.test(t))play(sounds.mission);else if(/徽章/.test(t))play(sounds.badge);else play(sounds.button)});export{play,sounds};
