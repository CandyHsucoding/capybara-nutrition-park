import{foods}from'../../data/foods';import{groups}from'../../data/nutrition';export function createRound(random=Math.random){const missing=groups[Math.floor(random()*groups.length)];return{missing,answer:foods.find(f=>f.group===missing)!,present:foods.filter(f=>f.group!==missing)}}export const next=(correct:boolean)=>correct?'clue':'observe';export const isOver=(lives:number)=>lives<=0;
